{
  "$schema": "https://json-schema.org/draft/2020-12/schema",

  "title": "TaskCreatedReplicationEvent.v1",
  "description": "JSON schema for task data replication (version 1).",

  "definitions": {
    "eventData": {
      "title": "TaskCreatedReplicationEventData.v1",
      "type": "object",
      "properties": {
        "taskId": {
          "description": "A globally unique ID of a task.",
          "type": "string",
          "format": "uuid"
        },
        "assigneeId": {
          "description": "A globally unique ID of an assignee a task belongs to.",
          "type": "string",
          "format": "uuid"
        },
        "taskDescription": {
          "description": "Description of a task.",
          "type": "string"
        },
        "creationDate": {
          "description": "Date-time of task creation.",
          "format": "date-time"
        }
      },

      "required": [
        "taskId",
        "assigneeId",
        "taskDescription",
        "creationDate"
      ]
    }
  },

  "type": "object",

  "properties": {
    "eventName":    { "type": "string", "const": "task.entity.created" },
    "eventVersion": { "enum": [1] },
    "eventId":      { "type": "string", "format": "uuid" },
    "eventTime":    { "type": "string" },
    "producer":     { "type": "string" },

    "data": {
      "$ref": "#/definitions/eventData"
    }
  },

  "required": [
    "eventName",
    "eventVersion",
    "eventId",
    "eventTime",
    "producer",
    "data"
  ]
}
